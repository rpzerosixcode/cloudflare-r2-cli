# CLI

A CLI será responsável por capturar e interpretar as mensagens fornecidas pelo usuário, executar as ações correspondentes e apresentar a resolução ao usuário.

Será utilizada a gem `thor` como abstração para definição e execução dos comandos.
