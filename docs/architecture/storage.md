# Armazenamento

O armazenamento será responsável por abstrair a comunicação com a API do Cloudflare R2.

Será utilizada a gem `aws-sdk-s3` para realizar a comunicação com o serviço de armazenamento.
